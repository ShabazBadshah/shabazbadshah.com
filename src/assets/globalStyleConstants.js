export const globalThemeColor = '#0089FF';
export const darkModeThemeColor = '#212121';
// Used to shift body "width" pixels left so it is not overlapped by the header (see header.js and layout.js)
export const headerWidthPx = 80;
